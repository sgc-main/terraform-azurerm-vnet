# Complete Example

Full-featured VNet demonstrating all module capabilities: NSG rules, Azure Firewall routing, custom delegations, Private Link, and extended service endpoints.

## Usage

```bash
terraform init
terraform plan -var-file="terraform.tfvars"
terraform apply -var-file="terraform.tfvars"
```
